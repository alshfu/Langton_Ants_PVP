// tests/unit/matchmaker/srWindow.test.ts

import { describe, it } from 'vitest';

describe('srWindow', () => {
  it.todo('returns initial window for waitMs < 10s');
  it.todo('expands to 150 after 10s');
  it.todo('expands to 300 after 30s');
  it.todo('caps at max after 60s');
});
